apiVersion: v1
kind: ServiceAccount
metadata:
  name: shovel-heroes-backend
  namespace: shovel-heroes
  annotations:
    # IRSA - IAM Role for Service Account (replace with actual role ARN from Terraform output)
    eks.amazonaws.com/role-arn: BACKEND_POD_ROLE_ARN
  labels:
    app.kubernetes.io/name: shovel-heroes-backend
    app.kubernetes.io/component: backend
