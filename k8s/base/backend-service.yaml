apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: shovel-heroes
  labels:
    app.kubernetes.io/name: shovel-heroes
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: shovel-heroes
    app.kubernetes.io/component: backend
  ports:
  - name: http
    port: 8787
    targetPort: 8787
    protocol: TCP
