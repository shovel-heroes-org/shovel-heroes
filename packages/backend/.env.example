DATABASE_URL=postgres://postgres:postgres@localhost:5432/shovelheroes
PORT=8787

# Public base URLs
PUBLIC_APP_URL=http://localhost:5173
PUBLIC_BASE_URL=http://localhost:8787

# LINE Login
LINE_CHANNEL_ID=
LINE_CHANNEL_SECRET=
LINE_REDIRECT_URI=

# Auth / Security
AUTH_JWT_SECRET=dev-jwt-secret-change-me
COOKIE_SECRET=dev-cookie-secret-change-me
COOKIE_SECURE=0

# Cloudflare Turnstile (server-side secret only)
TURNSTILE_SECRET_KEY=

# CORS Configuration (Production)
# Comma-separated list of allowed origins for production environment
# Example: ALLOWED_ORIGINS=https://shovel-heroes.netlify.app,https://app.example.com
ALLOWED_ORIGINS=https://shovel-heroes.netlify.app

# Notes
# 1. LINE_REDIRECT_URI if empty derives from PUBLIC_BASE_URL/auth/line/callback
# 2. Rotate AUTH_JWT_SECRET / COOKIE_SECRET if compromised
# 3. Set COOKIE_SECURE=1 when serving via HTTPS
# 4. Provide TURNSTILE_SECRET_KEY to enforce challenge on POST /grids
# 5. ALLOWED_ORIGINS controls CORS whitelist in production (development uses localhost:5173,5174)
